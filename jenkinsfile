pipeline {
    agent {
        label 'jenkins-docker-slave'
    }
    stages {
        stage('Checkout Code') {
            steps {
                git url: 'https://github.com/moranelb/super-app.git', branch: 'main', credentialsId: 'github-creds'
            }
        }
        stage('Build Docker Images') {
            steps {
                script {
                    // Build Docker images
                    sh '/usr/local/bin/docker-compose build' // Updated path for docker-compose
                }
            }
        }
        stage('Test Application') {
            steps {
                script {
                    // Run Docker Compose
                    sh '/usr/local/bin/docker-compose up -d' // Updated path for docker-compose
                    
                    // Add your test commands here, for example:
                    sleep(30) // wait for the containers to start
                    sh 'curl -f http://localhost:3000' // test Node.js app
                    sh 'curl -f http://localhost' // test PHP app
                }
            }
        }
        stage('Push to Docker Hub') {
            steps {
                script {
                    // Log in to Docker Hub
                    withCredentials([usernamePassword(credentialsId: 'dockerhub-cred', usernameVariable: 'DOCKER_USERNAME', passwordVariable: 'DOCKER_PASSWORD')]) {
                        sh 'echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin'
                    }
                    // Tag and push images
                    sh '/usr/local/bin/docker-compose push' // Updated path for docker-compose
                }
            }
        }
    }
}
